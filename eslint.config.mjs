export default [
  {
    ignores: ["node_modules/**", ".next/**", "dist/**", "build/**", "coverage/**"],
  },
  // Next.js uses legacy `.eslintrc.json` in this repo. This file only suppresses interactive prompts.
]

